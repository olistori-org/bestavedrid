{
  "name": "besta-vedrid",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint",
    "bootstrap": "python scripts/bootstrap.py",
    "scrape:locations": "python scripts/scrape_locations.py",
    "seed:locations": "python scripts/seed_locations.py"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
