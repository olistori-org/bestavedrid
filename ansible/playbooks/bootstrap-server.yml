- name: Bootstrap server
  hosts: web
  become: true

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install basic packages
      apt:
        name:
          - curl
          - git
          - ca-certificates
          - gnupg
          - lsb-release
        state: present